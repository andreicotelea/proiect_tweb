import * as react_jsx_runtime from 'react/jsx-runtime';
import { AxiosInstance, AxiosError, InternalAxiosRequestConfig, AxiosRequestConfig } from 'axios';
import * as react from 'react';

type Token = string;
type TokenStorage = {
    get: () => Token | undefined;
    set: (token?: Token) => void;
};
type RefreshResult = {
    accessToken: Token;
};
type NotifyLevel = "info" | "success" | "warning" | "error";
type NotifyPayload = {
    level: NotifyLevel;
    message: string;
    code?: string;
    status?: number;
    url?: string;
};
type RetriableRequestConfig = InternalAxiosRequestConfig & {
    _retry?: boolean;
    _skipNotify?: boolean;
};
type ShouldRetry = (error: AxiosError, config: RetriableRequestConfig) => boolean;
type ShouldRefresh = (error: AxiosError, config: RetriableRequestConfig) => boolean;
type IsRefreshRequest = (config: InternalAxiosRequestConfig) => boolean;
type OnAuthFailure = (reason: {
    error?: unknown;
    lastToken?: string;
}) => void;
type OnNotify = (payload: NotifyPayload) => void;
type GetTimeZone = () => string;
type BuildAuthHeader = (token: Token) => string;
type CreateAxiosInstance = (params: {
    baseURL: string;
}) => AxiosInstance;
type RefreshAccessToken = (params: {
    refreshClient: AxiosInstance;
    token: Token;
}) => Promise<RefreshResult>;
type ApiClient = {
    get: <T = any>(url: string, config?: AxiosRequestConfig) => Promise<T>;
    post: <T = any>(url: string, data?: any, config?: AxiosRequestConfig) => Promise<T>;
    put: <T = any>(url: string, data?: any, config?: AxiosRequestConfig) => Promise<T>;
    patch: <T = any>(url: string, data?: any, config?: AxiosRequestConfig) => Promise<T>;
    delete: <T = any>(url: string, config?: AxiosRequestConfig) => Promise<T>;
    postBlob: (url: string, data?: any, config?: AxiosRequestConfig) => Promise<Blob>;
    uploadFile: <T = any>(url: string, formData: FormData, config?: AxiosRequestConfig) => Promise<T>;
};
type AxiosProviderProps = {
    children: React.ReactNode;
    /**
     * Base URL for both main and refresh clients.
     */
    baseURL: string;
    /**
     * Token storage adapter (sessionStorage, localStorage, memory, etc.)
     */
    tokenStorage?: TokenStorage;
    /**
     * Default: Intl.DateTimeFormat().resolvedOptions().timeZone
     */
    getTimeZone?: GetTimeZone;
    /**
     * Default: token => `Bearer ${token}`
     */
    buildAuthHeader?: BuildAuthHeader;
    /**
     * Custom factory for axios instances.
     */
    createMainClient?: CreateAxiosInstance;
    /**
     * Custom factory for refresh client (often withCredentials true).
     */
    createRefreshClient?: CreateAxiosInstance;
    /**
     * Called when 401 happens and refresh fails (or no token).
     * You can redirect to login, clear app state, etc.
     */
    onAuthFailure?: OnAuthFailure;
    /**
     * Optional UI notification hook (toast/i18n is implemented in your app, not here).
     */
    onNotify?: OnNotify;
    /**
     * Decide if we should attempt refresh on this error.
     * Default: status === 401 and not already retried.
     */
    shouldRefresh?: ShouldRefresh;
    /**
     * Decide if we should retry for non-refreshable cases.
     * Default: false (only refresh flow does retries).
     */
    shouldRetry?: ShouldRetry;
    /**
     * Identify refresh requests to avoid refresh loops.
     * If you use refreshAccessToken that calls /auth/refresh, mark it here.
     * Default: config.url includes "/auth/refresh"
     */
    isRefreshRequest?: IsRefreshRequest;
    /**
     * Your refresh implementation (library doesn't know your endpoint/body).
     */
    refreshAccessToken?: RefreshAccessToken;
    /**
     * Expose the helper `api` wrapper (data-unwrapping)
     * Default: true
     */
    exposeApi?: boolean;
};
type AxiosContextValue = {
    client: AxiosInstance;
    refreshClient: AxiosInstance;
    token?: Token;
    setToken: (token?: Token) => void;
    api?: ApiClient;
};

declare function AxiosProvider({ children, baseURL, tokenStorage, getTimeZone, buildAuthHeader, createMainClient, createRefreshClient, onAuthFailure, onNotify, shouldRefresh, shouldRetry, isRefreshRequest, refreshAccessToken, exposeApi, }: AxiosProviderProps): react_jsx_runtime.JSX.Element;

declare function useAxios(): AxiosContextValue;

declare const AxiosContext: react.Context<AxiosContextValue | undefined>;

declare function createSessionStorageTokenStorage(key: string): TokenStorage;
declare function createLocalStorageTokenStorage(key: string): TokenStorage;
declare function createMemoryTokenStorage(initial?: string): TokenStorage;

type CreateApiOptions = {
    onNotify?: OnNotify;
};
declare function createApi(client: AxiosInstance, opts?: CreateApiOptions): ApiClient;

export { type ApiClient, AxiosContext, type AxiosContextValue, AxiosProvider, type AxiosProviderProps, type NotifyPayload, type RefreshAccessToken, type TokenStorage, createApi, createLocalStorageTokenStorage, createMemoryTokenStorage, createSessionStorageTokenStorage, useAxios };
